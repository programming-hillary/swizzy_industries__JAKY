import { BusinessDetailsModel } from './business-details-model';

describe('BusinessDetailsModel', () => {
  it('should create an instance', () => {
    expect(new BusinessDetailsModel()).toBeTruthy();
  });
});
