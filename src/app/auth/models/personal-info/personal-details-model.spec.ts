import { PersonalDetailsModel } from './personal-details-model';

describe('PersonalDetailsModel', () => {
  it('should create an instance', () => {
    expect(new PersonalDetailsModel()).toBeTruthy();
  });
});
